Google App Engine Java Runtime SDK - Some Familiar Haunts Demo

Allows users to mark locations with custom names. Uses the Google Maps API and
stores location information in the App Engine datastore. 

To compile and run this sample on the local dev server, run ant in this
directory as follows:

ant runserver

Once the server is running, point your web browser at one of the following
to try out this sample:

http://127.0.0.1:8080/

If you want to run this on Google App Engine, you will need to register for
a new Maps API key for your appspot subdomain. You can register for an API
key here:

http://code.google.com/apis/maps/signup.html
